{
  "name": "painel-sprints-jira",
  "private": true,
  "version": "1.0.0",
  "description": "Painel de acompanhamento de sprints do Jira, alimentado via MCP jira-desenv",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "test": "npm run test --workspace server && npm run test --workspace client",
    "install:all": "npm install"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
